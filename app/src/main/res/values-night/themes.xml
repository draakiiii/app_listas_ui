<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.App_listas" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/darkGrey</item> <!-- Barra superior y color botones -->
        <item name="colorPrimaryVariant">@color/darkGreyv2</item> <!-- Contenido items y menús flotantes -->
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/lightGrey</item> <!-- Valores secundarios item -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/darkGrey</item> <!-- Barra superior de estado Android -->
        <!-- Customize your theme here. -->
        <item name="android:buttonStyle">@style/Widget.MaterialComponents.Button</item>
    </style>
    <style name="LinearLayoutMargin" parent="Theme.App_listas">
        <item name="android:paddingLeft">100dp</item>
    </style>
</resources>