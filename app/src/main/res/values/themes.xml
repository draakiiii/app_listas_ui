<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->

    <style name="Theme.App_listas" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="android:textColor">@color/black</item>
        <item name ="colorOnPrimaryContainer">@color/darkWhite</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/grey</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
        <item name="android:buttonStyle">@style/Widget.MaterialComponents.Button</item>
        <item name="backgroundColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:buttonBarButtonStyle">@style/DatePickerTheme.ButtonBarNegativeButtonStyle</item>
    </style>

    <style name="LinearLayoutMargin" parent="Theme.App_listas">
        <item name="android:layout_marginLeft">100dp</item>
        <item name="android:maxWidth">500px</item>
    </style>

    <style name="CustomMaterialDialog" parent="@style/ThemeOverlay.MaterialComponents.MaterialAlertDialog">
        <!-- Background Color-->
        <item name="android:background">?attr/colorPrimaryVariant</item>
        <!-- Text Color for title and message -->
        <item name="colorOnSurface">?attr/colorOnPrimary</item>
        <!-- Text Color for buttons -->
        <item name="colorPrimary">?attr/colorOnPrimary</item>
    </style>

    <style name="DatePickerTheme.ButtonBarNegativeButtonStyle" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">?attr/colorOnPrimary</item>
    </style>

</resources>